# Changelog

## [v1.9.1] - 2026-09-07

### 修复 / Fixed

- **限速显示语义纠正** — 原"CPU 限速"读 SMC 键 `MSAc`，实测（floe/smc_util 键表 + 本机压测）它是 SMC 发出的 PLIMIT **滚动平均值**（诊断值），一次钳制后能滞后显示几十分钟，不是当前限速。现改为：瞬时值取 SMC **PLIMIT 命令**（0=不限，显示 100%），读取失败时先用 30s 最后好值缓存，再退回 `MSAc`；`SMCService.readPLimit()` 返回 Optional 并加 3 次重试，不再把"读取失败"当成"未限制"
- **新增 GPU 当前限制** — 独显被 SMC 钳制的比例现在可见（实测 A2141 上见过 gpu=7%）

### 优化 / Improved

- 限速面板 / 设置限速卡片 / 历史曲线头部统一为：**当前限制（CPU/GPU）+ 近期平均（MSAc，仅参考）+ 热状态**，并更新说明文案（纠正"空闲限速低=省电"的旧解释）
- 菜单栏限速段数值源同步替换为瞬时 PLIMIT，红字条件不变（限速 <95 且 CPU 负载 ≥50）

### 文档 / Docs

- 新增 [docs/a2141-thermal-throttling.md](docs/a2141-thermal-throttling.md)：A2141 温控与卡顿详解（SMC/PLIMIT/VRM 原理、温控策略表、本机压测数据、"读不到 VRM 温度为何还会压功率"、缓解方案与改装指南）

---

## [v1.7.0] - 2026-04-02

### 修复 / Fixed

- **电池数据显示异常** — 修复新版 macOS 下电池功率 0.0W、电压 0.0V、循环次数 0 的问题。新版 macOS 将 Voltage、CycleCount 等字段迁移至 BatteryData 子字典，顶层字段返回 0，现已增加 fallback 读取
- **充电状态误判** — 修复 IsCharging 报 No 但实际在充电的问题，通过 InstantAmperage 正数 + 插电状态综合判断

---

## [v1.6.0] - 2026-03-21

### 优化 / Improved

- **CPU 进程命令行** — 进程排行面板命令行列显示完整命令行参数（路径+启动参数），使用 sysctl KERN_PROCARGS2 替代仅返回路径的 proc_pidpath
- **进程面板响应速度** — 应用启动时预初始化 CPU 进程采样，首次打开面板即可立即显示数据，无需等待

---

## [v1.5.0] - 2026-03-19

### 新增 / Added

- **电池百分比显示** — 电池供电时在自定义电池图标内显示百分比数字，充电时仍使用系统闪电图标

### 优化 / Improved

- **电池文字可读性** — 百分比数字颜色自动适配深色/浅色模式，在填充区与未充电区均清晰可辨
- **电池未充电区域** — 提高未充电部分底色对比度，与填充区域视觉层次更分明
- **电池正极帽** — 缩小正极突起宽度，比例更协调

---

## [v1.4.0] - 2026-03-17

### 新增 / Added

- **GPU 使用率** — IOKit IOAccelerator 读取，支持多 GPU（集成显卡/独立显卡），状态栏显示最大利用率，tooltip 分行显示每个 GPU 的使用率
- **GPU 温度** — IOAccelerator PerformanceStatistics 读取，fallback SMC（AMD→TGDD，Intel→TCGC），tooltip 分行显示每个 GPU 的温度
- **GPU 开关** — 设置面板新增 GPU 使用率和 GPU 温度两个独立开关

### 优化 / Improved

- **默认开关调整** — 首次安装仅开启 CPU 使用率和网络速度，其余模块默认关闭，减少初始状态栏占用
- **图标区分度** — GPU 温度使用 `thermometer.sun.fill`，与 CPU 温度 `thermometer.medium` 区分

---

## [v1.3.1] - 2026-03-17

### 新增 / Added

- **能耗排行** — 电池弹框中显示 Top 3 高能耗 App（图标+名称），每 5 秒自动刷新
- **设置面板版本号** — 标题栏显示当前版本号和构建号

### 修复 / Fixed

- 部分机型（bq40z651 电池控制器）电池百分比与系统不一致，fallback 到 IOPSCopyPowerSourcesInfo
- 电池供电时功率显示 `--`（PDTR 残留值导致），改为仅在插电时读取 PDTR
- Amperage 读取改用 NSNumber.int64Value，修复部分机型有符号转换错误
- 电池弹框刷新后 UI 尺寸跳变，改为固定行预创建 + 原地更新

---

## [v1.3.0] - 2026-03-16

### 新增 / Added

- **CPU 使用率开关** — 设置面板新增 CPU 使用率模块开关，可独立控制显示/隐藏
- **电池真实电量图标** — 电池供电时状态栏显示自定义电池图标，内部白色填充按实际剩余电量比例绘制，定时更新

### 优化 / Improved

- **图标区分度提升** — CPU 图标改用 `cpu.fill`（实心），内存保持 `memorychip`（线框），一重一轻视觉区分更清晰（与 Stats app 一致）

### 修复 / Fixed

- CPU 使用率模块开关无效（setVisibility 中误跳过 .cpu 类型）

---

## [v1.2.0] - 2026-03-13

### 新增 / Added

- **Finder 右键菜单** — 基于 FinderSync 扩展，右键文件/文件夹/空白处
  - "在此打开终端" — 在选中的目录（或文件所在目录）打开 Terminal.app
  - "复制路径" — 将完整路径复制到剪贴板，多选时逐行复制
  - 通过 DistributedNotificationCenter 实现扩展与主 app 的进程间通信
  - 打开的终端与系统原生行为完全一致（标题、提示符、关闭行为）
- **右键菜单开关** — 设置面板新增"Finder 右键菜单"开关，一键启用/禁用

### 构建 / Build

- build.sh 新增 FinderSync 扩展编译、签名和打包步骤
- 扩展使用沙盒隔离，仅通过 DistributedNotification 与主 app 通信

---

## [v1.1.0] - 2026-03-12

### 新增 / Added

- **进程排行面板** — 点击 CPU / 内存 / 网络指标，弹出 Top 10 进程列表
  - CPU 面板：按 CPU 占用排序，显示进程名、PID、CPU%、命令行
  - 内存面板：按内存占用排序，显示进程名、PID、内存占用、命令行
  - 网络面板：按连接数排序，显示进程名、PID、连接数、LISTEN 端口、命令行
  - 支持点击列头排序（升序/降序切换）
  - 支持点击"查看"按钮弹出完整命令行
  - 面板标题和列头随语言切换实时更新
- **IP 归属地** — 网络进程面板新增"归属地"列
  - 集成 ip2region 离线数据库（~10MB，纯 C 库，0 网络依赖）
  - 中文模式自动翻译国外国名（United States → 美国，约 70 个国家）
  - 中文模式自动翻译常见 ISP 名称
- **连接服务** — 使用 libproc C API 获取进程网络连接信息（TCP/UDP/LISTEN）
- **可点击标签组件** — ClickableLabel 通用组件，支持进程面板交互

### 优化 / Improved

- **CPU 占用大幅降低** — 从 ~87% 降至接近 0%
  - 状态栏合并为单个 NSStatusItem，使用自定义绘制替代多个独立 item
  - 刷新逻辑从 Task.detached 改为专用 DispatchQueue，避免创建过多线程
  - 增加变化检测，数据未变时跳过 UI 更新
- **Tooltip 交互修复** — 温度/风扇/功率等提示框点击外部区域即可关闭
  - 从 `.transient` 改为 `.applicationDefined`，配合全局鼠标事件监听
- **弹框水平滚动** — 进程面板内容超出宽度时显示水平滚动条
- **国际化完善** — 新增 `commandLine` / `viewButton` / `geoLocation` 等本地化字符串

### 修复 / Fixed

- 弹框标题切换语言后不更新
- 弹框内列头标题切换语言后不更新
- 中文模式下 IP 归属地仍显示英文国名
- Tooltip 需要点击两次才能关闭

### 构建 / Build

- build.sh 新增 ip2region C 源码编译和链接步骤
- 新增 ObjC 桥接头文件支持 Swift 调用 C 库

---

## [v1.0.0] - 2026-03-10

### 新增 / Added

- **CPU 使用率** — 实时百分比显示
- **CPU 温度** — SMC 读取，无需 root 权限
- **内存占用** — 已用/总量百分比
- **风扇转速** — 支持多风扇 RPM 显示
- **网络速度** — 上传/下载速度，自动单位换算（B/KB/MB）
- **充电功率** — SMC PDTR 实时适配器功率显示
  - 点击查看电池详情（充电功率、协商功率、电池功率、电流、电压、电量、循环次数、健康度）
- **独立设置入口** — 状态栏常驻齿轮图标
  - 各监控模块独立开关
  - 刷新间隔配置（1-10 秒）
  - 开机自启设置
  - 中文/英文切换
- **0 外部依赖** — 纯 Swift + IOKit + SMC，无第三方库
- **轻量高效** — 二进制约 780KB，内存占用极低
